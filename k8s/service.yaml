apiVersion: v1
kind: Service
metadata:
  name: simple-api
  namespace: simple-api
  labels:
    app: simple-api
spec:
  type: ClusterIP
  selector:
    app: simple-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
